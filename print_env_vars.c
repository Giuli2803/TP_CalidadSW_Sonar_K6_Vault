#include <stdio.h>

int main() {
    const char* password = PASSWORD_OF_VAULT;
    const char* username = USERNAME_OF_VAULT;

    printf("PASSWORD_OF_VAULT: %s\n", password);
    printf("USERNAME_OF_VAULT: %s\n", username);

    return 0;
}
